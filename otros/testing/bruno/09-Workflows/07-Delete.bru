meta {
  name: Delete workflow
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/workflows/{{lastWorkflowId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

docs {
  # DELETE /workflows/:id

  Response: `{"message": "Workflow deleted"}`.
}
