meta {
  name: Export
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/bitacora/export?format=json
  body: none
  auth: bearer
}

params:query {
  format: json
}

auth:bearer {
  token: {{authToken}}
}

docs {
  # GET /bitacora/export

  Exporta entries. Envelope `{data: [], total}`.
  Formatos posibles: `json`, `csv`, `xlsx` (verificar cuales acepta).
}
